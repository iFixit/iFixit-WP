<phone:PhoneApplicationPage 
    x:Class="iFixit7.GuideView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="768" d:DesignWidth="480"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent" DataContext="{Binding}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--holds the name of the device, then name of topic-->
        <StackPanel>
            <TextBlock x:Name="GuideTopic" Text="{Binding Topic}" Margin="0,0,0,-5" Style="{StaticResource PhoneTextSubtleStyle}"/>
            <TextBlock x:Name="GuideTitle" Text="{Binding Title}" Margin="0,0,0,0" Style="{StaticResource PhoneTextSubtleStyle}"/>
        </StackPanel>
        
        <!--ContentPanel - place additional content here-->
        <controls:Pivot x:Name="GuidePivot" Grid.Row="1" ItemsSource="{Binding}">

            <!--the style for each title of the pivot of guides-->
            <controls:Pivot.TitleTemplate>
                <DataTemplate>
                    <StackPanel>
                        <!--PhoneFontSizeExtraExtraLarge margin 0,50,0,0-->
                        <TextBlock Text="{Binding}"
                                   FontSize="{StaticResource PhoneFontSizeExtraLarge}"
                                   Margin="0,0,0,0" />
                        <Rectangle Fill="Orange" Height="1" HorizontalAlignment="Stretch" />
                    </StackPanel>
                </DataTemplate>
            </controls:Pivot.TitleTemplate>

            <!-- the special info column -->
            <controls:PivotItem x:Name="GuideInfo" Header="Info">
                <StackPanel>
                    <ProgressBar x:Name="LoadingBarInfo" IsIndeterminate="True" Visibility="Visible" Foreground="Orange"/>

                    <Image x:Name="TitleImage" Source="{Binding TitleImage}"  />
                    <TextBlock x:Name="Summary" Text="{Binding Summary}" TextWrapping="Wrap" />
                </StackPanel>
            </controls:PivotItem>

            <!-- template for each step column -->
            <controls:Pivot.ItemTemplate>
                <DataTemplate>
                    <ListBox ItemsSource="{Binding ColContent, Mode=OneWay}" x:Name="ListItem" 
                             FontSize="{StaticResource PhoneFontSizeMedium}"
                             VerticalAlignment="Stretch">

                        <TextBlock Text="some step text"/>
                    </ListBox>
                </DataTemplate>
            </controls:Pivot.ItemTemplate>
        </controls:Pivot>
    </Grid>
</phone:PhoneApplicationPage>
